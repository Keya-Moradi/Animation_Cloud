# Session Configuration
SECRET_SESSION=your_super_secret_session_key_change_this_in_production

# External API Keys
GOOEY_API_KEY=your_gooey_api_key_here

# Database Configuration
# For local development, you don't need to set DATABASE_URL
# Sequelize will use the config.json settings instead
# For production (Heroku), this will be set automatically
DATABASE_URL=postgres://username:password@localhost:5432/animationcloud

# Server Configuration
NODE_ENV=development
PORT=13000
